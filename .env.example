# Optional: protect /api/scrape so only Vercel Cron or you can trigger it.
# SCRAPE_SECRET=replace-with-a-long-random-string

# Optional: use Vercel Blob to persist events.json.
# BLOB_READ_WRITE_TOKEN=vercel-blob-token

# Optional: Brave Search API key for the web search scraper.
# Get a free key (2000 queries/month) at https://brave.com/search/api
# BRAVE_API_KEY=your-brave-api-key
