interface:
  display_name: "Grenadaevents"
  short_description: "Repo-specific patterns and workflows for grenadaevents"
  default_prompt: "Use the grenadaevents repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true